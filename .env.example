# Este .env.example NÃO é lido por nenhum dos apps — Next.js e NestJS só leem
# .env de dentro da própria pasta. Use:
#   cp apps/api/.env.example apps/api/.env
#   cp apps/web/.env.example apps/web/.env.local
# e preencha os dois (os valores do Supabase se repetem nos dois arquivos).
